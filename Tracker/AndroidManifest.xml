<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.jedenpies.android.tracker"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="15" />
	
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />
	<uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
		
		<activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_main" >
        </activity>        
        <activity
            android:name=".listold.MyListActivity"
            android:label="@string/title_activity_main" >
        </activity>
        
		<service 
            android:name=".service.TrackerService" 
            android:exported="false"          
            android:permission="pl.jedenpies.android.tracker.service.TRACKER_SERVICE"
            android:process=":tracker_service">            
            <intent-filter >
                <action android:name="pl.jedenpies.android.tracker.TRACKER_SERVICE"/>
            </intent-filter>            
        </service>
		
    </application>

</manifest>